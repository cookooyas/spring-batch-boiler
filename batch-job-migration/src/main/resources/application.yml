spring:
  datasource:
    # common의 정보를 참조하여 할당
    source: "db-pool.mysql-db"
    target: "db-pool.oracle-db"
  batch:
    jdbc:
      initialize-schema: always # MariaDB에 배치 메타 테이블을 자동으로 생성합니다.
    job:
      enabled: true